{
  "name": "@techbuddy/api",
  "version": "0.0.1",
  "private": true,
  "description": "TechBuddy backend — Fastify, Anthropic SDK. All Claude calls live here.",
  "type": "module",
  "main": "src/server.ts",
  "engines": {
    "node": ">=20.10.0"
  },
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "prisma generate && tsc",
    "start": "prisma migrate deploy && node dist/server.js",
    "lint": "echo skipped",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.40.0",
    "@fastify/cors": "^10.0.1",
    "@prisma/client": "^5.22.0",
    "@sentry/node": "^8.45.0",
    "dotenv": "^16.4.5",
    "fastify": "^5.1.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "pino-pretty": "^11.3.0",
    "prisma": "^5.22.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
